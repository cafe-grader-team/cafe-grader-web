---
description: Learn project conventions by reading CLAUDE.md
---
// turbo-all
1. Use the `view_file` tool to read the `CLAUDE.md` file located at the root of the workspace.
2. Always strictly adhere to the database logic, server commands, and frontend UI conventions (such as the `.mi` class for icons) specified inside `CLAUDE.md`.

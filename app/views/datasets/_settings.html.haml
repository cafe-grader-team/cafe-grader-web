-# require *ds* as a dataset object
-#= turbo_frame_tag 'dataset_settings', data: {'problem-target': 'datasetSettings', 'base-url': settings_dataset_path(-123)} do
= simple_form_for ds, wrapper: :horizontal_form, multipart: true do |f|
  %div{'data-controller': 'init-ui-component'}
    %input{type: 'hidden', name: 'active_dataset_tab', value: @active_dataset_tab, data: {'bs-tab-target': 'activeTabInput'}}
    .row
      .col-12
        .d-flex.justify-content-between.align-items-center.my-2
          %h5.mb-0 Basic Info
          = link_to audit_logs_path(auditable_type: 'Dataset', auditable_id: ds.id), class: 'btn btn-sm bg-white shadow-sm border-0 text-secondary d-inline-flex align-items-center justify-content-center', data: {bs_toggle: 'tooltip', bs_title: 'Change history (incl. testcases)'} do
            %span.mi history
        = f.input :name
        = f.input :time_limit, label: "Time limit (s)", tooltip: 'Decimal is ok, e.g., 1.2'
        = f.input :memory_limit, label: "Memory Limit (MB)"
        = f.input :score_type, collection: [['[SUM] The score is the sum of all testcase','sum'],['[GROUP MIN] The score is summation of minimum of each group','group_min'],['[RAW SUM] The score is the direct sum of checker testcase scores','raw_sum']], include_blank: false, wrapper: :horizontal_select
        = f.input :evaluation_type, collection: Dataset.evaluation_types.keys, include_blank: false, wrapper: :horizontal_select
        = f.input :main_filename, collection: ds.managers.joins(:blob).pluck(:filename), include_blank: false, wrapper: :horizontal_select, disabled: ds.problem.compilation_type == 'self_contained'
        = f.input :initializer_filename, collection: ds.initializers.joins(:blob).pluck(:filename), include_blank: '-- No initializers --', wrapper: :horizontal_select, disabled: ds.initializers.blank?
        = f.button :submit, class: 'btn-primary'

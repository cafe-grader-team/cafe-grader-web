.card.mb-3.shadow-sm{data: {turbo: true}}
  .card-body
    .row.mb-3
      .col-6
        %h5.mb-0 
          Helper
          - if @submission
            = link_to 'Refresh', submission_comments_path(@submission,{for: 'edit'}), data: {turbo: "true", turbo_prefetch: false}, class: 'btn btn-outline-secondary btn-sm'
      .col-6.text-end
        .text-end
          %a{href: "#", data: { bs_toggle: :modal, bs_target: '#helpers_help' } } [ What's this? ]

    -# problems comment section (hint / solution / etc)
    = turbo_frame_tag :problem_hints do
      = render partial: 'problems/hints', locals: {problem: @problem}

    -# Submission comments section (AI assist / actual comment)
    = turbo_frame_tag :submission_comments do
      = render partial: 'submissions/comments', locals: {submission: @submission, show_edit: false}
  -# GenAI request
  - if @models && @models.any? && GraderConfiguration['system.llm_assist']
    .card-footer
      = render partial: 'add_assist', locals: {models: @models}

-# usage modal
.modal.fade#helpers_help{tabindex: -1,role: 'dialog'}
  .modal-dialog{role:'document'}
    .modal-content
      .modal-header
        %h4 What are helpers?
        %button.btn-close{type: 'button', data: {bs: {dismiss: :modal}}, aria: {label: 'Close'}}
      .modal-body
        There are two kinds of helpers.
        %ul
          %li.mb-2
            %strong Problem Hints
            are hints for this specific problem. The hint is pre-built according to 
            the problem description without considering your submission.
          %li.mb-2
            %strong Submission Assistant
            is a specific helps on this particular submission.
            The help is generated by analyzing your submission and evaluation result
            mainly by an LLM.
